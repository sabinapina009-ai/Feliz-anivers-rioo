<!DOCTYPE html>
<html lang="pt">
<head>
<meta charset="UTF-8">
<title>Feliz Aniversário!</title>
<style>
  body {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    height: 100vh;
    font-family: Arial, sans-serif;
    background: linear-gradient(135deg, #ff9a9e, #fad0c4);
    text-align: center;
  }
  #bolo {
    font-size: 100px;
    display: none;
    animation: aparecer 1s forwards;
  }
  #mensagem {
    font-size: 40px;
    color: #c2185b;
    display: none;
    margin-top: 20px;
    animation: aparecer 1s forwards;
  }
  @keyframes aparecer {
    from {opacity: 0; transform: scale(0.5);}
    to {opacity: 1; transform: scale(1);}
  }
  button {
    font-size: 20px;
    padding: 10px 20px;
    margin-top: 20px;
    cursor: pointer;
    border: none;
    border-radius: 10px;
    background-color: #ff6f91;
    color: white;
  }
  button:hover {
    background-color: #ff4c70;
  }
</style>
</head>
<body>

<button onclick="mostrar()">Clique aqui para ver!</button>

<div id="bolo">🎂🎉🎂</div>
<div id="mensagem">Feliz aniversário Crisvalda!</div>

<script>
function mostrar() {
  document.getElementById("bolo").style.display = "block";
  document.getElementById("mensagem").style.display = "block";
}
</script>

</body>
</html>
